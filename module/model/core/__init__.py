"""Core base classes and interfaces for all model implementations.

This module provides abstract base classes that define the common interface
for standard models (BaseEstimator), post-processing fairness models (PostBase),
and Rashomon set enumeration methods (RsetBase).
"""
